import type { Specification } from "./types";

export const pwaSpecs: Specification = {
  // "application shell exposes install metadata": async (dsl) => {
  //   const args = await dsl.pwa.given["the application shell"]();
  //   await dsl.pwa.then["the web manifest exposes install metadata"](args);
  // },
  // "first-time browser visit registers a service worker": async (dsl) => {
  //   const args = await dsl.pwa.given["a first-time browser visit"]();
  //   await dsl.pwa.then["a service worker is registered"](args);
  // },
  // "application shell meets installability prerequisites": async (dsl) => {
  //   const args = await dsl.pwa.given["the application shell in Chromium"]();
  //   await dsl.pwa.then["the app meets installability prerequisites"](args);
  // },
};
